# Contributing

1. Create a branch for one focused change.
2. Keep API keys, local file paths, generated bundles and `node_modules` out of commits.
3. Run `npm run build` and `cd src-tauri && cargo test` before opening a pull request.
4. Describe the user-visible result and how you tested it.
